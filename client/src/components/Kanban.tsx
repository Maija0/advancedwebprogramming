import { Box } from "@mui/material";

const Kanban = () => {
    return (
     <div>   
    <Box
    sx={{
        display: 'flex',
        justifyContent: 'flex-start',
        borderRadius: 1,
        flexDirection: "column",
    }}
    >
    <h1>Kanban -page</h1>
    </Box>
    </div>   

    )
}

export default Kanban;